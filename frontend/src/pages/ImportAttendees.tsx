import { Text } from '@chakra-ui/react';
import Page from '../components/Page/Page'

export default function ImportAttendees() {
  return (
    <Page
      pageTitle="Import Attendees"
      pageDescription="Create a new meetup by importing a list of attendees.">
        <Text>To-Do</Text>
    </Page> 
  );
}
