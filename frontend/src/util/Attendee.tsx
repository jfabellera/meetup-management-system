export interface Attendee {
    id: string
    name: string
    order_id: string
    checked_in: boolean
    raffle_number: number
    raffle_winner: boolean
    meetup_name: string
    email: string
}
